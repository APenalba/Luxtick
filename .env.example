# =============================================================================
# LuxTick - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# =============================================================================

# -- Telegram Bot --
TELEGRAM_BOT_TOKEN=your-telegram-bot-token-from-botfather

# OpenAI (GPT-4o) - used for receipt vision parsing
# Get yours at: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key

# -- Database User Credentials (for Docker Compose) --
# Used by docker-compose.yml and docker-compose.dev.yml
POSTGRES_USER=bot
POSTGRES_PASSWORD=your_very_secure_password_here
POSTGRES_DB=luxtick

# -- Database --
# For production, use strong passwords and secure server
# Usernames and passwords can be configured via POSTGRES_USER, POSTGRES_PASSWORD env vars
DATABASE_URL=postgresql+asyncpg://bot:your_secure_password@your-db-host:5432/luxtick
# Read-only connection for analytics queries (text-to-SQL)
DATABASE_URL_READONLY=postgresql+asyncpg://bot_readonly:your_secure_readonly_password@your-db-host:5432/luxtick

# -- Bot Settings --
# Webhook URL for production (leave empty for polling mode in development)
BOT_WEBHOOK_URL=
# Secret token for webhook verification
BOT_WEBHOOK_SECRET=a-random-secret-string

# -- Model Configuration --
# Override default models if desired
CONVERSATIONAL_MODEL=gemini/gemini-2.0-flash
VISION_MODEL=gpt-4o
ITEM_INTELLIGENCE_MODEL=gpt-4o-mini

# -- Feature Flags --
# Set to false to disable LLM-powered post-processing/categorization
ENABLE_ITEM_INTELLIGENCE=true

# -- Rate Limiting --
# Max messages per user per minute
RATE_LIMIT_PER_MINUTE=20

# -- Logging --
LOG_LEVEL=INFO
